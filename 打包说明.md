# 打包说明

把成绩登记做成免安装的独立程序：对方电脑不用装 Python、不用装任何依赖、不用联网，双击就能用。语音模型（sense-voice，最准的那个）已经打进程序里。

## 成品

| 平台 | 产物 | 体积 |
|------|------|------|
| macOS | `dist/成绩登记.app`，`dist/成绩登记.dmg` | 342MB / 压缩后 202MB |
| Windows | `dist\GradeRegister\` 整个文件夹 | 约 340MB |

## 怎么打包

**macOS** —— 双击 `构建-macOS.command`（或终端里 `./构建-macOS.command`）。自动建虚拟环境、装依赖、下模型、打包、生成 DMG。

**Windows** —— 双击 `构建-Windows.bat`。同样是全自动，只要机器上装了 Python 3.10+（安装时勾上 "Add Python to PATH"）。

**没有 Windows 电脑** —— 用 GitHub Actions：把代码推上去，在仓库的 Actions 页面点 `打包` → `Run workflow`，几分钟后在 Artifacts 里下载 Windows 和 macOS 两个包。配置在 `.github/workflows/build.yml`。

> PyInstaller 不能交叉编译。Windows 的 .exe 必须在 Windows 上生成，macOS 的 .app 必须在 macOS 上生成，没有例外。

## 怎么发给别人

**Windows**：把 `dist\GradeRegister` 整个文件夹压缩发过去，对方解压后双击 `GradeRegister.exe`。文件夹里的东西都要在，不能只拷 exe。

**macOS**：发 `成绩登记.dmg`。对方打开后把图标拖进 Applications。

由于没有花钱买苹果的开发者签名，对方**第一次**打开会被系统拦住（提示"无法打开，因为无法验证开发者"）。让他这样做一次：

> 右键点程序图标 → 选「打开」 → 在弹出的框里再点「打开」

之后就跟正常软件一样双击即可。或者在终端里执行 `xattr -cr /Applications/成绩登记.app` 一次性解除。

## 首次运行

程序第一次录音时，系统会弹窗请求麦克风权限，**必须点允许**，否则录不到声音。

- macOS：权限在「系统设置 → 隐私与安全性 → 麦克风」里可以随时改
- Windows：在「设置 → 隐私和安全性 → 麦克风」里

## 文件都放在哪

程序自身是只读的，用户数据分开存放：

| 内容 | 位置 |
|------|------|
| 配置（设置里的选项） | macOS `~/Library/Application Support/GradeRegister/config.json`<br>Windows `%APPDATA%\GradeRegister\config.json` |
| 语音模型 | 程序包内部，随程序分发 |
| 成绩表备份 | 跟着 xlsx 走，存在表格所在目录的 `backups/` 里 |

卸载就是直接删程序，配置目录可以一并删掉。

## 打包了什么、没打包什么

**打进去的**：sense-voice 语音模型（228MB，识别最准的那个）、sherpa-onnx 与 onnxruntime 运行库、PortAudio 录音库、tkinter 界面、openpyxl。

**故意排除的**：vosk、faster-whisper、sherpa 流式模型、torch、scipy、pandas、matplotlib。这些要么用不到、要么是备用引擎，加起来 1.5GB 以上，全部排除后包体积从 2GB 降到 342MB。程序只用内置的 sense-voice 识别。

## 出问题时

打包脚本报错，先看这几条：

- **模型缺失**：spec 会直接报错并提示先跑 `python download_model.py`
- **打包成功但双击没反应**：命令行跑 `dist/GradeRegister/GradeRegister.exe --check`（macOS 是 `dist/成绩登记.app/Contents/MacOS/成绩登记 --check`），它会打印依赖、模型、麦克风的自检结果
- **提示找不到麦克风**：先确认系统权限给了，再在软件的「设置」里手动选设备
